# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- `mutation-frontend` CI job: PRs now run Stryker only against files changed vs. the base branch (`git diff` + `--mutate`); pushes to `main`/`develop` run the full suite in incremental mode (`incremental: true` in `frontend/stryker.config.mjs`), with `reports/stryker-incremental.json` cached per branch across runs — cuts the ~30min full-repo mutation run on every PR down to just the touched code

### Added
- Repository scaffold: go.mod, Makefile, directory structure, .gitignore
- Wails v2 + React + TypeScript desktop shell (`wails dev`/`wails build`)
- CI workflow (`.github/workflows/ci.yml`): tests with coverage, 80% coverage gate, security-suppression check, `golangci-lint`, `govulncheck` on every push and PR to `main`/`develop`
- Release workflow (`.github/workflows/release.yml`): cross-platform `wails build` (Windows, Linux) and GitHub Release publishing on `v*` tags
- Frontend quality gate: ESLint, Prettier, and Vitest + React Testing Library with an 80% coverage threshold, wired into `frontend/package.json` scripts and the `quality-gate` CI job
- `secret-scan` CI job: full-history `gitleaks` scan on every push and PR
- `commit-lint` CI job: validates every commit message on a PR against the Conventional Commits format
- `.githooks/commit-msg` hook: local Conventional Commits validation, installed via `make install-hooks`
- `.github/dependabot.yml`: weekly automated update PRs for Go modules, npm packages, and GitHub Actions
- Expanded `.golangci.yml`: added `revive`, `gocritic`, `bodyclose`, `sqlclosecheck`, `misspell` linters and a `formatters` block (`gofmt`, `goimports`)
- `frontend/scripts/postinstall.mjs`: writes a throwaway `go.mod` into `frontend/node_modules` after every install, firewalling it from the root Go module so stray `.go` files shipped inside npm packages (e.g. `flatted`) don't break `go build`/`go vet`/`go test`/`golangci-lint` from the repo root
- Mutation testing: [Gremlins](https://github.com/go-gremlins/gremlins) for `internal/domain`/`internal/application` (`.gremlins.yaml`, `make mutation-go`) and [StrykerJS](https://stryker-mutator.io/) for `frontend/src` (`frontend/stryker.config.mjs`, `make mutation-frontend`/`npm run mutation`), wired as required `mutation-go`/`mutation-frontend` CI jobs; see [ADR-002](specs/decisions/ADR-002-mutation-testing.md)
- `AGENTS.md`: TDD commit gate and Go code rules now require no surviving mutants in changed `internal/domain`/`internal/application` code before commit
- Mocking strategy: [Mockery](https://vektra.github.io/mockery/) wired up for the Go backend (`.mockery.yaml`, `make mock`), generating mocks into a sibling `mocks` subpackage excluded from the coverage gate (`ci.yml`, `.githooks/pre-commit`) and from `make mutation-go` (`.gremlins.yaml`); frontend keeps Vitest's native `vi.mock()`/`vi.fn()` with no added library; see [ADR-003](specs/decisions/ADR-003-mocking-strategy.md)
- Local auth core (`internal/domain/auth`, `internal/application/auth`): `Account` model, `AccountRepository`/`SessionStore` ports, and `Register`/`Login`/`ResetLocalAccount` use cases, backed by a SQLite `AccountRepository` (`internal/infrastructure/sqlite`, pure-Go `modernc.org/sqlite` driver) and a JSON session marker at `~/.athena/session.json` (`internal/infrastructure/session`); see [01-auth-backend.md](specs/phases/phase-01-desktop-mvp/01-auth-backend.md)
- Desktop auth UI (`internal/interfaces/desktop`, `frontend/src/screens`): `Login`, `Register`, `ResetLocalAccount` and `HasLocalSession` Wails bindings wired to the local auth service, plus Login/Register/Reset-local-account React screens and a placeholder main screen so a new user lands directly in the app and an existing user (or an existing local session) skips straight past the login screen; see [02-auth-ui.md](specs/phases/phase-01-desktop-mvp/02-auth-ui.md)
- Frontend design system: Tailwind CSS v4 + shadcn/ui (`frontend/components.json`, `frontend/src/components/ui`), with a custom dark theme (`frontend/src/style.css`) evoking the Aegean night sky, marble and laurel gold, self-hosted Cinzel + Nunito fonts, and a hand-drawn Greek key divider component (`frontend/src/components/greek-key-divider.tsx`)
- Athena logo (`frontend/src/components/athena-logo.tsx`): a hand-drawn SVG monogram — a laurel wreath around a geometric "A" — shown on every screen (`AuthLayout`, `MainScreen`); plus a boot animation (`frontend/src/components/splash-screen.tsx`) where the Greek key motif slides in from all four screen edges before the monogram fades in, shown while `App` resolves the post-auth view
- LLM service (`internal/domain/llm`, `internal/infrastructure/openrouter`): `Provider` port (`Chat`/`ChatStream`/`Embeddings`) implemented by an OpenRouter HTTP `Client` with hand-rolled SSE streaming, a task-type → tier → model router (`TierFor`/`ModelFor`, cheap/medium/premium), and per-call usage tracking (`UsageRecorder` port, SQLite-backed `UsageRepository`, new `usage` table) recording tokens and cost after every call; insufficient-credits responses from `Chat`/`ChatStream` retry once against OpenRouter's `openrouter/free` auto-router and flag the fallback on the response; see [05-llm-service.md](specs/phases/phase-01-desktop-mvp/05-llm-service.md)
- Study Mode (`internal/domain/study`, `internal/application/study`, `frontend/src/screens/StudyScreen.tsx`): `Session`/`Message` model with SQLite-backed `SessionRepository`/`MessageRepository` (new `sessions`/`messages` tables), a `Service` with `Start`/`RequestOpeningTurn`/`SendMessage`/`End` use cases that build the study system prompt from the current `UserProfile` and stream each LLM turn via `Provider.ChatStream`, persisting the user's message before the call and the assistant's full reply only once its stream completes (never partial content); session creation (`Start`) is a separate, fast call from streaming the opening turn (`RequestOpeningTurn`), so the chat view appears immediately instead of waiting for the whole first response to finish generating; the system prompt explicitly directs the model to open with one short question and stay conversational rather than lecture, matching the spec's question-answer-feedback flow; new Wails bindings (`StartStudySession`/`RequestOpeningTurn`/`SendStudyMessage`/`EndStudySession`) emit `study:chunk`/`study:done`/`study:error` events — the first use of Wails runtime events in this codebase — consumed by a new chat-style `StudyScreen` (topic entry → streaming message list → reply box → end session), unlocking the previously-locked "Study" section in the app shell; see [06-study-mode.md](specs/phases/phase-01-desktop-mvp/06-study-mode.md)
- Settings screen (`internal/application/onboarding`, `internal/interfaces/desktop/settings.go`, `frontend/src/screens/SettingsScreen.tsx`): lets the user edit every `UserProfile` field and the OpenRouter key after onboarding, without re-running it. A new `Service.UpdateProfile` use case reuses onboarding's `profiles`/`config`/`validator` ports but preserves the profile's original `CreatedAt` instead of restamping it, unlike onboarding's `SaveProfile`; a new `UpdateProfile` Wails binding returns the saved (trimmed) profile so the frontend can sync local state without a second `GetProfile` round trip. The OpenRouter key gained a genuine "changes take effect immediately" fix: `openrouter.Client` now guards its API key behind a `sync.RWMutex` with a `SetAPIKey` method (new `domainllm.APIKeyUpdater` port), and `App.SaveOpenRouterKey` — already shared between onboarding's key gate and Settings via the existing `OpenRouterKeyForm` component — calls it right after a successful save, so an already-running study session picks up the new key without an app restart. The new `SettingsScreen` reuses `OpenRouterKeyForm` verbatim and the same Input/Select/TagInput field components as onboarding's form, now sourced from a shared `frontend/src/lib/profileOptions.ts` instead of duplicated option lists; unlocks the previously-locked "Settings" section in the app shell. See [08-settings.md](specs/phases/phase-01-desktop-mvp/08-settings.md)
- Study session folders (`internal/domain/folder`, `internal/application/folder`, `frontend/src/components/study-folder-tree.tsx`): sessions can now be grouped into folders, like ChatGPT Projects. A new `folders` table is seeded with a default "General" folder every session falls back to (`Session` gains a `FolderID`, always populated); `SessionRepository` gains `GetByID`/`ListByFolder`/`Reopen`/`MoveToFolder`/`ReassignFolder`, and a new `folder.Service` covers create/rename/delete (deleting reassigns sessions to "General" first; "General" itself can't be deleted) and list. New Wails bindings expose folder CRUD plus `ResumeStudySession` (reopens an ended session and returns its full history — sessions are no longer a dead end once ended) and `MoveStudySession`/`ListStudySessionsByFolder`. On the frontend, folders and sessions now live in the sidebar as an explorer-style tree (`StudyFolderTree`) instead of a topic-entry form on the Study screen itself: expand a folder to browse or start a session in it, move a session between folders, or reopen an ended one and keep chatting. `StudyScreen.tsx` (renamed `StudyChatScreen.tsx`) now only renders the chat itself, driven by `app-shell.tsx`, which owns which session is open; see [10-study-folders.md](specs/phases/phase-01-desktop-mvp/10-study-folders.md)

### Changed
- Study Mode chat redesign (`frontend/src/screens/StudyScreen.tsx`, new `frontend/src/components/message-bubble.tsx`): messages now render as chat bubbles distinguished by color and alignment only (laurel-gold `bg-primary` bubbles for the user, `bg-card` bubbles for the assistant, no avatars) instead of unstyled aligned text, with message content parsed as Markdown (`react-markdown` + `remark-gfm`) — headings, lists, links, tables and fenced code blocks with `react-syntax-highlighter` syntax highlighting themed to the Athena dark palette — instead of raw plain text; the in-flight `streamingText` renders through the same Markdown pipeline as it accumulates, not just once settled
- Raised the mutation testing break threshold from 60% to 80% for both `make mutation-go` (`.gremlins.yaml` `unleash.threshold.efficacy`) and `make mutation-frontend`/`npm run mutation` (`frontend/stryker.config.mjs` `thresholds.break`); Stryker's `mutate` glob now also excludes vendored shadcn/ui code (`frontend/src/components/ui/**`, `frontend/src/lib/utils.ts`) to match the coverage gate
- Moved the Wails entrypoint from `cmd/athena/` to root `main.go`: Wails v2 does not support a main package outside the project root

### Fixed
- Window opening minimised/unfocused on Linux (confirmed on Cinnamon/Muffin): `gtk_window_present` — what backs Wails' `WindowUnminimise`/`WindowCenter` — gets silently downgraded to a taskbar "demands attention" flash by focus-stealing prevention when the app is launched from a terminal instead of a desktop launcher, and toggling always-on-top hits the same wall. `main.go`'s `OnStartup` now shells out to `wmctrl -F -a "Athena"` (matched by the window's exact title, a literal so it satisfies gosec's G204 with no suppression, retried for a few seconds to survive slow window mapping), whose activation request carries the EWMH "pager" source indicator that these window managers do trust